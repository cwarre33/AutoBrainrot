# Unified requirements for AutoBrainrot: OpenClaw + video-gen (automated-content-generator)
# Kept current for 2026; moviepy 1.x for compatibility with existing pipeline (moviepy.editor API).

# --- Video / media (video-gen pipeline) ---
moviepy>=1.0.3,<2
imageio>=2.31.1
imageio-ffmpeg>=0.4.8
mutagen>=1.46.0
Pillow>=10.0.0
numpy>=1.25.2,<3
proglog>=0.1.10

# --- Reddit (optional; used if running full Reddit scrape path) ---
praw>=7.7.1
prawcore>=2.3.0

# --- TTS / speech (video-gen uses Streamlabs Polly; gTTS for optional local use) ---
gTTS>=2.5.0

# --- HTTP / API ---
requests>=2.31.0
certifi>=2023.7.22
charset-normalizer>=3.2.0
idna>=3.4
urllib3>=2.0.4

# --- Instagram / YouTube upload (video-gen) ---
instagrapi>=1.19.7
pycryptodomex>=3.18.0
pyotp>=2.9.0
PySocks>=1.7.1

# --- Browser automation (title/comment images) ---
selenium>=4.11.2
trio>=0.22.2
trio-websocket>=0.10.3
sniffio>=1.3.0
h11>=0.14.0
outcome>=1.2.0
wsproto>=1.2.0
websocket-client>=1.6.2
sortedcontainers>=2.4.0
typing_extensions>=4.7.1

# --- CLI / UX ---
rich>=13.5.2
colorama>=0.4.6
tqdm>=4.66.1

# --- Config / env ---
python-dotenv>=1.0.0

# --- Other (video-gen / OpenClaw) ---
attrs>=23.1.0
cffi>=1.15.1
decorator>=4.4.2
exceptiongroup>=1.1.3
markdown-it-py>=3.0.0
mdurl>=0.1.2
pycparser>=2.21
Pygments>=2.16.1
pydantic>=1.10.9
update-checker>=0.18.0
